// Enum for defining the status of a page
enum PageStatus {
    PUBLISHED // ACTIVE
    ARCHIVED  // INACTIVE
    TRASHED   // REMOVED
}

// Enum for defining the general status of an entity
enum Status {
    ACTIVE
    INACTIVE
    TRASHED
}


enum otpOrigin {  // Enum for defining the origin of OTP
    MFA_Login
    forgot_Pass
    NULL
}


enum logOutcome {  // Enum for defining the log outcome
    Success
    Failure
    Unknown
}
